{
  "name": "redis-cache",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm -s run clean && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ioredis": "^4.23.0",
    "tslib": "^2.1.0"
  },
  "devDependencies": {
    "@types/ioredis": "^4.22.0",
    "typescript": "^4.2.3"
  }
}
